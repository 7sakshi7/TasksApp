<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container-fluid">
    <a class="nav-link text-warning" href="#">MyNotesApp</a>
    <button
      class="navbar-toggler"
      type="button"
      data-bs-toggle="collapse"
      data-bs-target="#navbarSupportedContent"
      aria-controls="navbarSupportedContent"
      aria-expanded="false"
      aria-label="Toggle navigation"
    >
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <a
            style="font-size: 20px"
            aria-current="page"
            href="/"
            class="<%= path=== '/about' ? 'text-primary' : '' %> nav-link"
            >About Us</a
          >
        </li>
        <% if(isAuthenticated){ %>
        <li class="nav-item">
          <a
            style="font-size: 20px;"
            aria-current="page"
            href="/add-notes"
            class="<%= path=== '/add-notes' ? 'text-primary' : '' %> nav-link"
            >Add Notes</a
          >
        </li>
        <li class="nav-item">
          <a
            style="font-size: 20px;"
            aria-current="page"
            href="/mynotes"
            class="<%= path=== '/mynotes' ? 'text-primary' : '' %> nav-link"
            >My Notes</a
          >
        </li>
        <% } %>
      </ul>
      <% if(!isAuthenticated){ %>
      <a href="/login" class="text-decoration-none">
        <button
          type="submit"
          class="btn btn-outline-success mx-3"
          style="display: block; margin: auto"
        >
          Login
        </button>
      </a>
      <a href="/signup" class="text-decoration-none">
        <button
          type="submit"
          class="btn btn-outline-success mx-3"
          style="display: block; margin: auto"
        >
          Sign Up
        </button>
      </a>
      <% } else { %>
      <a href="/logout" class="text-decoration-none">
        <button
          type="submit"
          class="btn btn-outline-success mx-3"
          style="display: block; margin: auto"
        >
          Logout
        </button>
      </a>
      <% } %>
    </div>
  </div>
</nav>
